<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <!-- google fontsの導入 -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;700&family=Notable&display=swap" rel="stylesheet">
    <!-- Bootstrap Iconsの導入 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- chart.jsの導入 -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  </head>

  <body>
    <div>
      <div class="row">
        <!-- サイドバー -->
        <%= render 'layouts/sidebar' %>
        <!-- メイン -->
        <main class="<%= "col-md-9" unless ( yield(:title) == 'Home' || yield(:title) == 'Log in' || yield(:title) == 'Sign up' ) %>">
          <!-- ヘッダー -->
          <%= render 'layouts/header' %>
          <!-- コンテンツ -->
          <section class="page-content">
            <div class="container">
              <% flash.each do |message_type, message| %>
                <div class="alert alert-<%= message_type %>"><%= message %></div>
              <% end %>
              <%= yield %>
              <!-- development環境のみdebugを表示 -->
              <%= debug(params) if Rails.env.development? %>
            </div>
          </section>
      </div>
    </div>
  </body>
</html>
